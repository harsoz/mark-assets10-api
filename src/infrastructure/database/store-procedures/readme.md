store procedures should be created by external operation or during the initial load

we can come back here to separate this from the api

sp_getProjectsFilteredByType could be split into different procs to isolate projects